// Define the object
const student = {
    name: "Joe Blow",
    age: 25,
    country: "Canada"
};


const { name, age } = student;

// Print the formatted message using template literals
console.log(`Hello! My name is ${name} and I am ${age} years old.`);


